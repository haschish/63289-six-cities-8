export enum AppRoute {
  Main = '/',
  SignIn = '/login',
  Favorites = '/favorites',
  Room = '/offer/:id',
}

export enum AuthStatus {
  Authorized = 'authorized',
  NoAuthorized = 'noAuthorized',
}
